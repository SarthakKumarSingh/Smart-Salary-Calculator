{
  "name": "astro-microtool",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "deploy": "WRANGLER_SEND_METRICS=false wrangler pages deploy dist --project-name smart-salary-calculator --branch main --commit-dirty=true",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "@astrojs/check": "^0.9.9",
    "@astrojs/cloudflare": "^14.1.3",
    "@astrojs/sitemap": "^3.7.3",
    "astro": "^7.0.7",
    "typescript": "^6.0.3",
    "wrangler": "^4.112.0"
  }
}
